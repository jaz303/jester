import foo { alpha as a, beta as b, gamma as c }

-----

{ type: A.MODULE, body: [], ports: [
	{ 	type: A.IMPORT,
		bang: false,
		module: { type: A.IDENT, name: 'foo' },
		alias: null,
		imports: { alpha: "a", beta: "b", gamma: "c" }
	}
] }
