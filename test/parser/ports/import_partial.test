import foo { alpha, beta, gamma }

-----

{ type: A.MODULE, body: [], ports: [
	{ 	type: A.IMPORT,
		bang: false,
		module: { type: A.IDENT, name: 'foo' },
		alias: null,
		imports: { alpha: "alpha", beta: "beta", gamma: "gamma" }
	}
] }
